<view class="container">
  <view wx:if="{{loading}}" class="card">
    <view class="loading">加载中...</view>
  </view>

  <block wx:else>
    <view class="timetable-hero">
      <view>
        <view class="date-line">{{dateText}} {{weekdayText}}</view>
        <view class="week-line">{{weekText}} · {{weekTypeText}}</view>
      </view>
      <button class="refresh-btn" loading="{{syncing}}" disabled="{{syncing}}" bindtap="syncTimetable">刷新课表</button>
    </view>

    <view wx:if="{{!hasTimetable}}" class="card">
      <view class="card-title">暂无课表缓存</view>
      <view class="tips">暂无课表，请点击刷新课表。首次使用或新学期开始后，需要先同步本学期数据。</view>
    </view>

    <view wx:if="{{error}}" class="card error-card">
      <view class="error-text">{{error}}</view>
    </view>

    <view class="section-list">
      <view wx:for="{{sections}}" wx:key="section" class="lesson-card">
        <view class="lesson-head">
          <view class="lesson-title">{{item.title}}</view>
        </view>

        <view wx:if="{{item.courses.length === 0}}" class="no-class">无课</view>

        <view wx:for="{{item.courses}}" wx:for-item="course" wx:key="id" class="course-block">
          <view class="course-name">{{course.courseName}}</view>
          <view class="course-room">{{course.displayLocation || course.classroomRaw || '地点待定'}}</view>
          <view wx:if="{{course.teacherName || course.weeksRaw}}" class="course-meta">
            <text wx:if="{{course.teacherName}}">{{course.teacherName}}</text>
            <text wx:if="{{course.teacherName && course.weeksRaw}}"> · </text>
            <text wx:if="{{course.weeksRaw}}">{{course.weeksRaw}}</text>
          </view>
        </view>
      </view>
    </view>
  </block>
</view>
